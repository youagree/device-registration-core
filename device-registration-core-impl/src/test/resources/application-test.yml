spring:

  datasource:
    username: device_registration_admin
    password: squd

  liquibase:
    enabled: true
    parameters:
      schema: device_registration_service
      text_array_type: ARRAY
    change-log: classpath:database_changelog.xml
    drop-first: true
  cloud:
    discovery:
      enabled: false

logging:
  level:
    ROOT: INFO